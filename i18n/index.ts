export { default as en } from './en.json';
export { default as lv } from './lv.json';

// All the locales supported by the app, must match the available translation files.
export const LOCALES = ['en', 'lv'];
// The default locale to use if the user's requested language is not available.
export const DEFAULT_LOCALE = 'en';
